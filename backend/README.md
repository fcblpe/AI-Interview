# backend

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run index.ts
```

the goal of this project is make a socket server where user and gemini ai voice model can participate in real time interview.
